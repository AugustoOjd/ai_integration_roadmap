# Interpolated into docker-compose.yml, so credentials are written once.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=mini_db

# For the app running outside Docker. 5433 = host side of the port mapping.
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5433/mini_db
